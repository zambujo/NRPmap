---
title: "NRP Network Map"
output: 
  flexdashboard::flex_dashboard:
    theme: cerulean
    social: menu
    source_code: embed
    vertical_layout: fill
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
if (!require(pacman)) install.packages("pacman")
if (!require(janitor)) install.packages("janitor")
p_load(
  "flexdashboard",
  "visNetwork",
  "tidyverse", 
  "emojifont",
  "lubridate", 
  "stringr", 
  "magrittr", 
  "rio")
NRP_NUMBER <- 72
```

Inputs {.sidebar}
-------------------------------------

<br>

> **Data**:  
> [P3_GrantExport.csv](http://p3.snf.ch/Pages/DataAndDocumentation.aspx)  
> [P3_PersonExport.csv](http://p3.snf.ch/Pages/DataAndDocumentation.aspx)

<hr>

> **Credits**:  
> [flexdashboard](https://github.com/rstudio/flexdashboard), [visNetwork](https://datastorm-open.github.io/visNetwork/)

<hr><br>

Useful Links:  

 - [SNSF National Research programmes](http://www.snf.ch/en/researchinFocus/nrp/Pages/default.aspx)  
 - [NRP 72 on Antimicrobial Resistance](http://www.nfp72.ch/en/Pages/Home.aspx)

Column
-------------------------------------

### 

```{r}

# download the data -------------------------------------------------------

grants <- read.csv2(
  url("http://p3.snf.ch/P3Export/P3_GrantExport.csv"),
  stringsAsFactors = FALSE) %>%
  janitor::clean_names()
# grants <- import("grants.rds")

people <- read.csv2(
  url("http://p3.snf.ch/P3Export/P3_PersonExport.csv"),
  stringsAsFactors = FALSE) %>%
  janitor::clean_names()
# people <- import("people.rds")

# NRP grant selection -----------------------------------------------------

nrp <- grants %>%
  # basic info about the grant
  select(
    project_number, # unique identifier
    project_title,
    funding_instrument,
    university,
    approved_amount) %>%
  filter(str_detect(funding_instrument, str_c("^NRP ", NRP_NUMBER))) %>%
  mutate(
    # keep acronyms only
    university = str_extract(university, "(?<=- )[[:upper:]]+$"),
    approved_amount = as.numeric(approved_amount)) # deprecated with rio::?

# involved people ---------------------------------------------------------

people <- people %>% 
  select(
    person_id_snsf, 
    last_name, 
    first_name, 
    gender, 
    institute_place,
    projects_as_applicant, 
    projects_as_partner, 
    projects_as_employee) %>%
  unite(project_number, starts_with("projects"), sep = ";") %>%
  mutate(
    institute_place = str_replace_all(institute_place, " Cedex(.*)?| [0-9]{1,2}", ""),
    # unite() includes NAs...
    project_number = str_replace_all(project_number, "NA", ""),
    project_number = str_replace_all(project_number, "[;]+", ";"),
    project_number = str_replace_all(project_number, "^[;]|[;]$", ""),
    # to unnest
    project_number = str_split(project_number, ";")) %>% 
  unnest() %>%
  # for consistency with grants
  mutate(project_number = as.integer(project_number)) %>%
  distinct() %>%
  inner_join(nrp, by = "project_number")

# grant-node settings for visNetwork() ------------------------------------

nodes_grants <- nrp %>%
  mutate(
    label = project_number,
    group = "Grants",
    institute = university,
    gender = NA,
    title = str_c(
      '<a href=\"http://p3.snf.ch/project-', project_number, '\">', 
      str_wrap(project_title, width = 35), '</a>'),
    title = str_replace_all(title, "\\n", "<br>"),
    title = str_c('</font><br><font size=\"1\" color=\"#70a1d7\">', title, 
      '</font><br><font size=\"1\" color=\"#202020\">',
      round(approved_amount/1000), 'kCHF</font>'),
    shadow = FALSE,
    shape = "icon",
    icon.code = "f15c",
    icon.color = "#70a1d7",
    icon.size = log(approved_amount) * 12) %>%
  select(
    id0 = project_number,
    label, 
    group, 
    institute, 
    gender, 
    title, 
    shadow, 
    shape, 
    starts_with("icon"))

# people-node settings for visNetwork() -----------------------------------

nodes_people <- people %>%
  group_by(person_id_snsf) %>%
  summarise(
    label = str_c(first_name[1], " ", last_name[1]),
    group = "People",
    institute = NA,
    gender = gender[1],
    title = str_c(
      '<font size=\"1\" color=\"#4477AA\">', label, '</font>'),
    shadow = FALSE,
    shape = "icon",
    icon.code = "f007",
    icon.color = "#202020",
    icon.size = 120) %>%
  rename(id0 = person_id_snsf)

# build nodes & edges -----------------------------------------------------

nodes <- rbind(nodes_grants, nodes_people) %>%
  rownames_to_column("id") %>%
  ungroup()

edges <- people %>%
  distinct() %>%
  mutate(
    width = 5,
    color = "#808080") %>%
  left_join(nodes, by = c("person_id_snsf" = "id0")) %>%
  rename(from = id) %>%
  left_join(nodes, by = c("project_number" = "id0")) %>%
  rename(to = id) %>%
  select(from, to, width, color) %>%
  mutate(from = as.numeric(from), to = as.numeric(to))

# visNetwork() output -----------------------------------------------------

visNetwork(nodes, edges) %>%
  visIgraphLayout() %>%
  visPhysics(stabilization = FALSE) %>%
  visOptions(
    selectedBy = "institute",
    highlightNearest = list(enabled = TRUE, hover = TRUE)) %>%
  visInteraction(navigationButtons = TRUE)

```

> Last updated: `r Sys.time()`. Compiled with `r version$version.string`.
